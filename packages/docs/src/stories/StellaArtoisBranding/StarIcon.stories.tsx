import { StellaStarIcon } from '@getlove/react';
import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'Components/StellaArtoisBranding/StarIcon',
  component: StellaStarIcon,
  parameters: {
    docs: {
      description: {
        component:
          'This is the StellaStarIcon component, it shows a Stella Star Icon.',
      },
    },
  },
} as Meta;

export const Primary: StoryObj = {};
